# RSpec + Capybara + Selenium Framework


Generated scaffold based on user's original implementation.


## Setup
1. Copy `.env.example` to `.env` and fill credentials.
2. `bundle install`
3. `bundle exec rspec` to run tests.


## Notes
- Credentials are loaded from ENV or `config/test_data.yml`.
- Screenshots are attached to Allure results on failure.